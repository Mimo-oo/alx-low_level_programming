#include <stdio.h>
/**
 * main - print the name eof a program
 * @argc: number of command line arguements
 * @argv: name of array
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
