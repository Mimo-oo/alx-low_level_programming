#include "<stdio.h>"
#include "main.h"
/**
 * main - prints the name of the program
 * @argc: Number of command line arguement
 * @argv: holds value of string in the array
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	printf("*s\n", argv[0]);

	return (0);
}
